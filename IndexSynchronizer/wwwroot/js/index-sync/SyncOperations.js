const runningOperations = new Map();

// TODO: add logic for generating and storing operationIdentifier
// TODO: add logic to generate rows in, e.g. a table with operationIdentifiers with an associated "cancel" button (and maybe other metadata)